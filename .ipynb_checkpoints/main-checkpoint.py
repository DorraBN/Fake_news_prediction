import random
import numpy as np
import tensorflow as tf
from tensorflow.keras.models import Sequential  # Correct spelling for 'Sequential'
from tensorflow.keras.layers import LSTM, Dense, Activation  # Correct spelling for 'Activation'
from tensorflow.keras.optimizers import RMSprop
